// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  hmr: false,
  firebase: {
    apiKey: "AIzaSyDY6Pxf9A0aFBm-ZJdMRXJ_Vr7VzeoEL-8",
    authDomain: "reuse-app-a48aa.firebaseapp.com",
    databaseURL: "https://reuse-app-a48aa.firebaseio.com",
    projectId: "reuse-app-a48aa",
    storageBucket: "reuse-app-a48aa.appspot.com",
    messagingSenderId: "211394172835"
  }
};

/*
 * In development mode, to ignore zone related error stack frames such as
 * `zone.run`, `zoneDelegate.invokeTask` for easier debugging, you can
 * import the following file, but please comment it out in production mode
 * because it will have performance impact when throw error
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
